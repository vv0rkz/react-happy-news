# ШАБЛОН: CURRENT_INCREMENT.md

> Скопируй этот файл в CURRENT_INCREMENT.md, заполни по инструкции ниже.
> Удали все строки с `>` перед коммитом.

---

# US X.X.X — [Название User Story]

**Статус:** `active`
**Релиз:** [CURRENT_RELEASE.md](./CURRENT_RELEASE.md)
**Issue:** `#N`  ← создать через: npm run _ create-task "US X.X.X: Название"
**Покрывает вопросы:** QN (тема), FQN (тема), ...

> Вопросы берутся из ROADMAP.md раздела соответствующего релиза

**Acceptance Criteria:**

- [ ] ...
- [ ] ...

---

## Концепция

> Опиши что происходит с точки зрения пользователя и системы.
> Формат "стрелочная диаграмма" — хорошо читается:

```
Событие
  → следствие
  → следствие

Другой сценарий
  → ...
```

**Почему [технология X], а не [альтернатива Y]:**
> Одно-два предложения. Объясняет обоснование выбора.

---

## Git

**Ветка:** `vX.X.X-branch-name`
**Issue:** `#N`

---

## Архитектура

> FSD-структура затронутых файлов. Помечай: ← НОВАЯ ФИЧА, ← ИЗМЕНИТЬ, ✅ уже есть

```
server/src/
└── ...

client/src/
├── features/
│   └── feature-name/
│       ├── Component.tsx       ← НОВАЯ ФИЧА
│       ├── useHook.ts          ← НОВАЯ ФИЧА
│       └── index.ts
└── shared/
    └── util.ts                 ← НОВАЯ ФИЧА
```

**FSD:** `layer/feature` → импортируется в `layer/consumer` — слои соблюдены.

---

## Шаг N: Название

**Файл:** `path/to/file.ts`

```typescript
// Сигнатуры и псевдокод:
// 1. Что принимает
// 2. Что возвращает
// 3. Ключевая логика (шаги, не код)
```

**Подводный камень:** ...

```bash
git add <файлы шага>
git commit -m "feat: #N <что сделано>"
```

> Последний шаг закрывает issue: `feat: close #N <что сделано>`

> Повтори блок "## Шаг N" для каждого шага

---

## Подводные камни

> Общий список edge-cases и ловушек для всего инкремента:

- ...
- ...
